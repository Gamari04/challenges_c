#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, c;
    printf("saisir les valeurs de a b et c: ");
    scanf("%d %d %d",&a,&b,&c);
    printf("le nouveau classement des nombres sera: %d %d %d",c,b,a);
    return 0;
}
